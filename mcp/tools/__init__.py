"""Tool modules for the DevLake MCP server.

Each submodule registers its own tools against the shared FastMCP instance
in mcp/server.py. Importing the module is sufficient to register its tools.
"""
